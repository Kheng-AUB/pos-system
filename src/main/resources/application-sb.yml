spring:
  datasource:
    url: jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:5432/postgres?sslmode=require&connectTimeout=30&socketTimeout=60
    username: postgres.erfqosalcpqpqdqllabt
    password: 
    driver-class-name: org.postgresql.Driver

  hikari:
    maximum-pool-size: 8
    minimum-idle: 2
    connection-timeout: 45000      # increased
    validation-timeout: 15000
    idle-timeout: 60000

  jpa:
    hibernate:
      ddl-auto: update          # Change to "validate" or "none" when stable
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

