spring:
  application:
    name: pos

  datasource:
    url: jdbc:postgresql://localhost:5432/pos_sys_db
    username: postgres
    password: 123456
    driver-class-name: org.postgresql.Driver

  jpa: # ← move one level to the left
    hibernate:
      ddl-auto: update         # or update / validate / none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true

jwt:
  secret: Lg2wNGuFaSqTJlke2THH7vLwl6IHmZ5o8Ftl7tGFaMF

server:
  port: 8080